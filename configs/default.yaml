model:
  weights: "models/yolov8n.pt"
  conf_threshold: 0.25
  iou_threshold: 0.45
  device: "cpu"

pipeline:
  source: "data/demo_input.mp4"  # Path to your downloaded demo video
  enable_tracking: true         # Missing previously
  tracker_type: "bytetrack.yaml" # Missing previously
  save_output: false            # Missing previously
  show_window: true